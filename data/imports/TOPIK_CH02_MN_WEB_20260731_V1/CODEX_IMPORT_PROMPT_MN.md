# CODEX IMPORT PROMPT (MN)

Энэ багцын `topik_ch02_seed.json` файлыг импортлоход дараах зарчмыг баримтална:
1. `package_id`-г шалгах.
2. `source_pages_total == 20` байхыг баталгаажуулах.
3. `ranking_places_total == 40`, `ranking_conversation_topics_total == 20` байхыг шалгах.
4. `supplementary_ch1.exercises` нь 10 ширхэг байх.
5. `chapter2.expected_problem_sets` нь 8 байх.
6. Давхардал шалгахдаа `page_no + no` эсвэл `set_id + no` түлхүүр ашиглах.
